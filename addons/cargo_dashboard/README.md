# cargo_dashboard

Odoo backend dashboard overview for Cargo Marketplace administrators.

## What it does

- Provides a **Dashboard** menu item in the Cargo backend menu
- Embeds the Order Analytics graph from `cargo_reports` as the landing view
- No new models, no new REST endpoints

## Dependencies

`cargo_reports`
